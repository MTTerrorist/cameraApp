import React from "react";
import { View, Text } from "react-native";

const RadioGroup = sizes => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
const styles = StyleSheet.create({
  style
});

export default RadioGroup;
